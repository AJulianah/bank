Pour compiler le fichier, on utilise gcc:

gcc main.c -o main
_________________________

-------------------------
Execution:
-------------------------
Sur linux :
    On rend le fichier executable \n
        chmod +x main
    ,\n On l'execute \n
        ./main

Sous windows:
    .\main